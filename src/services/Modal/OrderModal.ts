export interface Order {
    id?: String,
    name: String,
    mobile:Number,
    address: String,
    city: String,
    zip: Number,
    country: String,
    currency: String,
    totalPrice: Number,
}

           